[build]
  command = "npm install && chmod +x node_modules/.bin/vite && node_modules/.bin/vite build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
